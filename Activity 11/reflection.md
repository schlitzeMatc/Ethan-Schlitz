# Activity 11 Reflection: React Tic-Tac-Toe

## Key Learnings
1. **React components make larger apps easier to manage. Each component focuses ona single responsibility instead of writing a long DOM code**
2. **Props are inputs that are passed from parent to child components. Because they are read only, it keeps data flow predictable**
3. **Event Handling are different from vanilla JS. You would have to add event listners manually but react events are attached directly in JSX**
## Comparison
1. **Instead of manually updating variables in activity 10, React updates the UI whenever state changes.**
2. **The React components are reusable, instead of rewritin gcode, certain components can be used multiple time without rewriting the code**
3. **Instead of editing the DOM after each move, the react is able to apply the necessary changes.  This saves time and makes editing the code easier.**
## Challenges
1. **JSX was difficult to use because it looks like HTML but acts differently.  I had to figure out how JavaScript was used inside of JSX**
2. **Using slice() was confusing at first, but is makes a lot of sense reusing the code/making copies instead of modifying arrays.**
3. **Understating why the state needs to move up to a parent component was confusing at first but after some time it became clear why it is so important**
## Next Steps
1. **I want to learn more about custom hooks**
2. **I want to fully understant how to pass props down multiply layers and learn how the Contect API works**
